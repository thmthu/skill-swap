import { router as authRouters } from './authRoutes.js';
import { router as userRouters } from './userRoutes.js';
import { router as connectionRouters } from './connectionRoutes.js';
import { router as chatRouters } from './chatRoutes.js';

export {
    authRouters,
    userRouters,
    connectionRouters,
    chatRouters
}; 